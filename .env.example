KEYCLOAK_CLIENT_SECRET=your_client_secret_here
VITE_CLIENT_SECRET=your_client_secret_here

# API proxy (Vite dev): backend origin
# VITE_API_PROXY_TARGET=http://localhost:8080

# SPA public path for built assets (Vite `base`). Use `/` when the app is served at the site root (default).
# Only set a subpath if you deploy under a prefix, e.g. VITE_BASE_PATH=/crm/
# VITE_BASE_PATH=/

# Optional: only the site origin for API. If set wrong, e.g. with an extra path segment,
# requests can 404. Leave unset in dev to use same-origin /api + Vite proxy.
# VITE_API_BASE_URL=https://hayyah.me

# API path after /api: omit for default /v1 (hayyah.me). Use empty for local Express `/api/*` only:
# VITE_API_PATH_PREFIX=

# Auth: in dev, prefer a relative path so Vite proxies /auth → Keycloak (see .env.development).
# Production builds often use the full token URL as a build arg instead.
# VITE_AUTH_TOKEN_URL=https://your-host/auth/realms/your-realm/protocol/openid-connect/token
# VITE_AUTH_PROXY_TARGET=https://your-host

# App Store / Play Store links (optional; shown on the public `/welcome` landing when set)
# VITE_IOS_APP_STORE_URL=https://apps.apple.com/app/your-app/id000000000
# VITE_PLAY_STORE_URL=https://play.google.com/store/apps/details?id=com.example.app
